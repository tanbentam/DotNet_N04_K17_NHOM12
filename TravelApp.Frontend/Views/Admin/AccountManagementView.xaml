<UserControl x:Class="TravelApp.Frontend.Views.Admin.AccountManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="+ Táº¡o Guide Account" Command="{Binding CreateGuideAccountCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="+ Táº¡o User Account" Command="{Binding CreateUserAccountCommand}" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Width="50" Height="50"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding UsersList}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" materialDesign:DataGridAssist.CellPadding="12"
                  Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Vai TrÃ²" Binding="{Binding Role}" Width="150"/>
                <DataGridTextColumn Header="Sá»‘ Äiá»‡n Thoáº¡i" Binding="{Binding PhoneNumber}" Width="150"/>
                <DataGridTemplateColumn Header="Thao TÃ¡c" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Sá»­a" Style="{StaticResource MaterialDesignFlatButton}" Padding="0"/>
                                <Button Content="XÃ³a" Command="{Binding DataContext.DeleteAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red" Padding="0" Margin="10,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
