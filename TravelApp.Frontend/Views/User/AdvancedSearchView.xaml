<UserControl x:Class="TravelApp.Frontend.Views.User.AdvancedSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Advanced Search"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <WrapPanel Grid.Row="1" Margin="0,0,0,18">
            <TextBox Width="180"
                     Margin="0,0,12,12"
                     Text="{Binding SearchProvince, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Province/city"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox Width="150"
                     Margin="0,0,12,12"
                     Text="{Binding SearchMaxPrice, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Max price"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox Width="140"
                     Margin="0,0,12,12"
                     Text="{Binding SearchMinRating, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Min rating"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox Width="180"
                     Margin="0,0,12,12"
                     Text="{Binding SearchGuideName, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Guide"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox Width="160"
                     Margin="0,0,12,12"
                     Text="{Binding SearchTime, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Time slot"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <Button Content="Search"
                    Width="120"
                    Height="40"
                    Command="{Binding SearchCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </WrapPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding SearchResults}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Destination" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Province" Binding="{Binding Province}" Width="*" />
                <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="*" />
                <DataGridTextColumn Header="Guide from" Binding="{Binding GuidePriceFrom}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
