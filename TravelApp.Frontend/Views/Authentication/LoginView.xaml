<UserControl x:Class="TravelApp.Frontend.Views.Authentication.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0F766E">
            <StackPanel VerticalAlignment="Center" Margin="72,0">
                <TextBlock Text="Travel App"
                           Foreground="White"
                           FontSize="46"
                           FontWeight="Bold" />
                <TextBlock Text="Plan destinations, guides, hotels, bookings, and payments from one WPF workspace."
                           Foreground="#D9FFF9"
                           FontSize="18"
                           TextWrapping="Wrap"
                           Margin="0,16,48,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="56">
            <materialDesign:Card Padding="32"
                                 VerticalAlignment="Center"
                                 UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Sign in"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <TextBlock Text="Use admin/admin for the default admin account."
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               Margin="0,0,0,24" />

                    <TextBox Text="{Binding Identifier, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Email or phone number"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,16" />

                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,20" />

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#B91C1C"
                               Margin="0,0,0,16"
                               TextWrapping="Wrap"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

                    <Button Content="SIGN IN"
                            Command="{Binding LoginCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Height="42" />

                    <Button Content="Create a customer account"
                            Command="{Binding GoToRegisterCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="0,12,0,0" />

                    <ProgressBar IsIndeterminate="True"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 Margin="0,16,0,0" />
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
