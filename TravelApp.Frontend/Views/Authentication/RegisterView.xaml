<UserControl x:Class="TravelApp.Frontend.Views.Authentication.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.95*" />
            <ColumnDefinition Width="1.05*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="56">
            <materialDesign:Card Padding="32"
                                 VerticalAlignment="Center"
                                 UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Create account"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <TextBlock Text="Customer self-registration creates a User role account."
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,24" />

                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Email"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,16" />

                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,16" />

                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Phone number (10 digits)"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,16" />

                    <TextBox Text="{Binding Province, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Current province/city"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,20" />

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#B91C1C"
                               Margin="0,0,0,16"
                               TextWrapping="Wrap"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

                    <Button Content="REGISTER"
                            Command="{Binding RegisterCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Height="42" />

                    <Button Content="Back to sign in"
                            Command="{Binding GoToLoginCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="0,12,0,0" />

                    <ProgressBar IsIndeterminate="True"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 Margin="0,16,0,0" />
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <Border Grid.Column="1" Background="#2563EB">
            <StackPanel VerticalAlignment="Center" Margin="72,0">
                <TextBlock Text="Start with a customer profile"
                           Foreground="White"
                           FontSize="38"
                           FontWeight="Bold"
                           TextWrapping="Wrap" />
                <TextBlock Text="Guide accounts are created by Admin only. Backend integration points are marked in the frontend services and view models."
                           Foreground="#EAF1FF"
                           FontSize="17"
                           TextWrapping="Wrap"
                           Margin="0,16,40,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
