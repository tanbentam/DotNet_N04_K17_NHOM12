TravelApp.Backend

Skeleton backend project for Travel App.

Structure mirrors TravelApp.Frontend: Contracts, Models, Services, Utils, ViewModels, Views, Controllers, Data, Migrations, Properties, Resources.

Next steps:
- Add Entity Framework 6 packages
- Configure MySQL connection string in `App.config` or `Web.config`
- Implement API controllers and services
